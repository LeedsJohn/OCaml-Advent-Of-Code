open! Core

(* let can_make_sum nums goal =
  let rec aux cur nums =
    if cur = goal then true
    else if cur > goal then false
    else
      match nums with
      | [] -> false
      | hd :: tl -> aux (cur + hd) tl || aux cur tl
  in
  aux 0 nums

let get_first_three_possibilities nums goal =
    let rec aux acc nums = *)

let part1 _ = Error (Error.of_string "Unimplemented")
let part2 _ = Error (Error.of_string "Unimplemented")
